export default class Explosion {
  constructor(x, y, parent) {
    this.x = x;
    this.y = y;
    this.game = parent;
    this.radius = 30;
  }

  update(dt) {
    // Update explosion logic here
  }

  draw(ctx) {
    // Draw explosion here
  }
}
