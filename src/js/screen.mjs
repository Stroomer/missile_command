// buffer for screen-rendering (params: id, width, height, alpha, bgcolor, visible, showAttribs)
let screen = createBuffer('screen', width, height, false, black, true, true); // onscreen context

// let screen = document.getElementById('screen');

// if (!screen) {

//   canvas = document.createElement('canvas');
//   document.body.appendChild(canvas);
// }

// export { canvas };
